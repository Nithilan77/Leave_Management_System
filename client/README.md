# Leave Management System — Frontend (Client)

The React (Vite) frontend. To be scaffolded next.

## Planned setup
```bash
npm create vite@latest . -- --template react
npm install
npm install axios react-router-dom
npm run dev   # starts on http://localhost:5173
```
