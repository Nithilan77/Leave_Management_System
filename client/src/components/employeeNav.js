/**
 * Navigation config for the Employee role.
 * Keeping this in one place means all three employee pages show the same nav,
 * and adding/renaming a link is a one-line change.
 */
export const employeeNav = [
  { label: 'Dashboard', path: '/' },
  { label: 'Apply for Leave', path: '/apply' },
  { label: 'My Requests', path: '/my-requests' },
];
